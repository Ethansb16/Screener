---
phase: 05-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/dashboard.test.js
  - src/web/queries.js
  - src/web/templates.js
autonomous: true
requirements:
  - DASH-01
  - DASH-02

must_haves:
  truths:
    - "listOpportunities() returns rows with company_name, signal_type, status, and top_signal_classification"
    - "getOpportunityDetail(id) returns opportunity with all 4 signals and claude_analysis"
    - "getOpportunityDetail(nonexistent) returns null"
    - "renderFeedPage() produces HTML containing an opportunity table with hx-get attributes"
    - "renderDetail() produces HTML containing all 4 signal names and claude_analysis text"
    - "renderDetail() with null claude_analysis shows fallback text, not the string 'null'"
    - "renderLayout() wraps content in full HTML document with HTMX and Tailwind CDN script tags"
  artifacts:
    - path: "src/__tests__/dashboard.test.js"
      provides: "Unit tests for queries and templates"
      min_lines: 80
    - path: "src/web/queries.js"
      provides: "DB query functions for feed and detail views"
      exports: ["listOpportunities", "getOpportunityDetail"]
    - path: "src/web/templates.js"
      provides: "HTML template functions for rendering"
      exports: ["renderLayout", "renderFeedPage", "renderDetail", "esc"]
  key_links:
    - from: "src/web/queries.js"
      to: "src/db/db.js"
      via: "import default db singleton"
      pattern: "import db from.*db/db\\.js"
    - from: "src/web/templates.js"
      to: "src/web/queries.js"
      via: "templates consume query result shapes"
      pattern: "opp\\.company_name|opp\\.claude_analysis|opp\\.signals"
---

<objective>
Create the data layer (queries) and presentation layer (templates) for the dashboard, plus Wave 0 failing tests that define the expected behavior for both DASH-01 and DASH-02.

Purpose: Establish testable contracts before wiring routes. Queries and templates are pure functions that can be tested without Express.
Output: Three new files — dashboard.test.js, queries.js, templates.js — with all tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard/05-RESEARCH.md
@.planning/phases/05-dashboard/05-VALIDATION.md

@src/db/db.js
@src/db/schema.js
@src/main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 tests + query functions (queries.js)</name>
  <files>src/__tests__/dashboard.test.js, src/web/queries.js</files>
  <read_first>
    - src/db/db.js (DB singleton import pattern)
    - src/db/schema.js (table schemas — filings, opportunities, signals columns)
    - src/__tests__/analyze.test.js (test pattern: import with ?v= cache buster, before/after hooks, test data setup/teardown)
  </read_first>
  <action>
    Create `src/web/queries.js` with two exported functions:

    1. `listOpportunities()` — returns all opportunities ordered by discovered_at DESC. SQL:
       ```sql
       SELECT o.id, o.company_name, o.signal_type, o.status,
              o.discovered_at, o.spinoff_target,
              s.classification AS top_signal_classification
       FROM opportunities o
       LEFT JOIN signals s
         ON s.filing_id = o.filing_id
        AND s.signal_name = 'reason_classification'
       ORDER BY o.discovered_at DESC
       ```
       Uses `db.prepare(...).all()`. Import db from `../db/db.js`.

    2. `getOpportunityDetail(id)` — accepts a numeric id. Returns null if not found. SQL for opportunity:
       ```sql
       SELECT o.*, f.company_name AS parent_company, f.filed_at, f.accession_number
       FROM opportunities o
       JOIN filings f ON f.id = o.filing_id
       WHERE o.id = ?
       ```
       Then fetches signals:
       ```sql
       SELECT signal_name, classification, confidence, raw_excerpt
       FROM signals
       WHERE filing_id = (SELECT filing_id FROM opportunities WHERE id = ?)
       ORDER BY signal_name
       ```
       Returns `{ ...opp, signals }` or `null`.

    Create `src/__tests__/dashboard.test.js` with tests:

    Test setup (before hook): Import db singleton with `?v=30` cache buster, call `initializeSchema()`, clean up test data (DELETE from signals, opportunities, filings WHERE accession_number = 'TEST-DASH-000001'), then insert:
    - 1 filing: accession_number='TEST-DASH-000001', form_type='10-12B', cik='0000088888', company_name='DashTest Corp', filed_at='2025-07-01'
    - 1 opportunity: filing_id from above, source_type='edgar', company_name='DashTest Corp', signal_type='spinoff', status='new', claude_analysis='Summary text.\n\nRed Flags:\n- Excessive debt loading'
    - 4 signals: (filing_id, 'reason_classification', 'strategic_focus', 'high'), (filing_id, 'equity_grants', 'equity_grants_confirmed', 'medium'), (filing_id, 'debt_loading', 'excessive_debt', 'high'), (filing_id, 'management_continuity', 'strong_leaders_moving', 'medium')

    Test teardown (after hook): DELETE test data in reverse order (signals, opportunities, filings).

    Import queries with `?v=30` cache buster.

    Tests for queries (describe 'queries'):
    - `listOpportunities returns rows with expected columns`: Call listOpportunities(), find row where company_name === 'DashTest Corp', assert it has properties: id, company_name, signal_type, status, top_signal_classification. Assert top_signal_classification === 'strategic_focus'.
    - `getOpportunityDetail returns full detail with signals`: Call getOpportunityDetail(testOppId), assert result is not null, assert result.company_name === 'DashTest Corp', assert result.claude_analysis contains 'Red Flags', assert result.signals.length === 4, assert result.signals.some(s => s.signal_name === 'reason_classification').
    - `getOpportunityDetail returns null for nonexistent id`: Call getOpportunityDetail(999999), assert result === null.
  </action>
  <verify>
    <automated>node --test src/__tests__/dashboard.test.js</automated>
  </verify>
  <acceptance_criteria>
    - src/web/queries.js contains `export function listOpportunities()`
    - src/web/queries.js contains `export function getOpportunityDetail(id)`
    - src/web/queries.js contains `import db from '../db/db.js'`
    - src/web/queries.js contains `LEFT JOIN signals s` (not INNER JOIN — handles missing signals)
    - src/web/queries.js contains `reason_classification` in the LEFT JOIN condition
    - src/__tests__/dashboard.test.js contains `describe('queries'`
    - src/__tests__/dashboard.test.js contains `TEST-DASH-000001` (test fixture accession number)
    - `node --test src/__tests__/dashboard.test.js` exits with code 0
  </acceptance_criteria>
  <done>Both query functions return correct shapes from the test DB. Three query tests pass: list returns rows with top_signal_classification, detail returns full object with 4 signals, nonexistent returns null.</done>
</task>

<task type="auto">
  <name>Task 2: Create template functions (templates.js) and template tests</name>
  <files>src/web/templates.js, src/__tests__/dashboard.test.js</files>
  <read_first>
    - src/web/queries.js (query return shapes — this task's templates consume these)
    - src/__tests__/dashboard.test.js (add template tests to this file)
    - .planning/phases/05-dashboard/05-RESEARCH.md (template patterns, signal labels, esc function, CDN URLs)
  </read_first>
  <action>
    Create `src/web/templates.js` with these exported functions:

    1. `esc(str)` — HTML escape function. If str is null/undefined, return ''. Otherwise: replace & with &amp;, < with &lt;, > with &gt;, " with &quot;, ' with &#39;. Return String(str) after replacements.

    2. `SIGNAL_LABELS` — exported const object mapping classification values to {label, color}:
       - strategic_focus: {label: 'Strategic Focus', color: 'text-green-700'}
       - weak_unit_disposal: {label: 'Weak Unit Disposal', color: 'text-red-700'}
       - mixed: {label: 'Mixed', color: 'text-yellow-700'}
       - equity_grants_confirmed: {label: 'Equity Grants', color: 'text-green-700'}
       - no_equity_grants: {label: 'No Equity Grants', color: 'text-red-700'}
       - no_debt_concern: {label: 'No Debt Concern', color: 'text-green-700'}
       - moderate_debt: {label: 'Moderate Debt', color: 'text-yellow-700'}
       - excessive_debt: {label: 'Excessive Debt', color: 'text-red-700'}
       - strong_leaders_moving: {label: 'Leaders Moving to SpinCo', color: 'text-green-700'}
       - leaders_staying_at_parent: {label: 'Leaders Staying at Parent', color: 'text-red-700'}

    3. `renderLayout(bodyContent)` — returns full HTML document string:
       - DOCTYPE html, lang="en"
       - meta charset UTF-8, viewport meta
       - title: "Spinoff Screener"
       - Script tag: `https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4`
       - Script tag: `https://cdn.jsdelivr.net/npm/htmx.org@2.0.8/dist/htmx.min.js`
       - body with class `bg-gray-50 text-gray-900 p-6`
       - bodyContent interpolated inside body

    4. `renderFeedPage(opportunities)` — accepts array of opportunity objects (from listOpportunities). Returns HTML string containing:
       - h1 "Spinoff Screener" with `text-2xl font-bold mb-4`
       - A table with thead containing columns: Company, Type, Status, Top Signal
       - Each opportunity as a `<tr>` with:
         - `hx-get="/opportunities/${opp.id}"` attribute
         - `hx-target="#detail-panel"` attribute
         - `hx-swap="innerHTML"` attribute
         - `hx-push-url="true"` attribute
         - class `cursor-pointer hover:bg-slate-100 border-b`
         - td cells for: esc(opp.company_name), esc(opp.signal_type), esc(opp.status), top signal label (use SIGNAL_LABELS[opp.top_signal_classification]?.label or 'Pending' if null, with color class)
       - After the table, a `<div id="detail-panel">` with placeholder text "Select a spinoff to view details."

    5. `renderDetail(opp)` — accepts opportunity detail object (from getOpportunityDetail). Returns HTML fragment:
       - h2 with esc(opp.company_name), class `text-xl font-bold mb-2`
       - dl or div with: Status (opp.status), Type (opp.signal_type), Discovered (opp.discovered_at), Filed (opp.filed_at), Accession (opp.accession_number)
       - h3 "Signal Breakdown" with `text-lg font-semibold mt-4 mb-2`
       - If opp.signals is empty, show "No signals extracted yet."
       - Otherwise a table with columns: Signal, Classification, Confidence
       - Each signal row: signal_name label, classification with color from SIGNAL_LABELS, confidence
       - h3 "AI Analysis" with `text-lg font-semibold mt-4 mb-2`
       - If opp.claude_analysis is null/undefined, show "Analysis pending — check back after the next pipeline run."
       - Otherwise: Split claude_analysis on "Red Flags:" — render the first part as a paragraph, then if a "Red Flags:" section exists, render it with class `text-red-700 font-semibold` header and each line starting with `-` as a list item. All text passed through esc().

    Add tests to `src/__tests__/dashboard.test.js` (append a new describe block 'templates'):

    Import templates with `?v=30` cache buster.

    Tests:
    - `esc escapes HTML entities`: assert esc('<b>"hi"</b>') === '&lt;b&gt;&quot;hi&quot;&lt;/b&gt;'
    - `esc returns empty string for null`: assert esc(null) === ''
    - `renderLayout includes HTMX and Tailwind CDN`: call renderLayout('<p>test</p>'), assert output includes 'htmx.org@2.0.8', includes '@tailwindcss/browser@4', includes '<!DOCTYPE html>', includes '<p>test</p>'
    - `renderFeedPage renders opportunity rows with hx-get`: create mock opportunity array [{id: 1, company_name: 'Test Corp', signal_type: 'spinoff', status: 'new', top_signal_classification: 'strategic_focus'}], call renderFeedPage, assert output includes 'hx-get="/opportunities/1"', includes 'Test Corp', includes 'Strategic Focus'
    - `renderFeedPage handles null top_signal_classification`: mock opp with top_signal_classification: null, assert output includes 'Pending'
    - `renderDetail renders all 4 signals`: create mock opp with signals array containing all 4 signal_name values, assert output includes 'reason_classification' or its label, includes 'equity_grants' or its label, includes 'debt_loading' or its label, includes 'management_continuity' or its label
    - `renderDetail shows fallback for null claude_analysis`: create mock opp with claude_analysis: null, assert output includes 'Analysis pending', does NOT include 'null' as standalone word (use regex /\bnull\b/ to check it does not appear)
    - `renderDetail renders red flags section`: create mock opp with claude_analysis: 'Summary here.\n\nRed Flags:\n- Debt stuffing detected', assert output includes 'Red Flags', includes 'Debt stuffing detected'
  </action>
  <verify>
    <automated>node --test src/__tests__/dashboard.test.js</automated>
  </verify>
  <acceptance_criteria>
    - src/web/templates.js contains `export function esc(`
    - src/web/templates.js contains `export function renderLayout(`
    - src/web/templates.js contains `export function renderFeedPage(`
    - src/web/templates.js contains `export function renderDetail(`
    - src/web/templates.js contains `export const SIGNAL_LABELS`
    - src/web/templates.js contains `htmx.org@2.0.8`
    - src/web/templates.js contains `@tailwindcss/browser@4`
    - src/web/templates.js contains `hx-get="/opportunities/`
    - src/web/templates.js contains `Analysis pending`
    - src/__tests__/dashboard.test.js contains `describe('templates'`
    - `node --test src/__tests__/dashboard.test.js` exits with code 0
  </acceptance_criteria>
  <done>All template functions produce correct HTML. Query tests still pass. Template tests verify: esc works, layout includes CDN tags, feed rows have hx-get attributes, null signals show "Pending", null claude_analysis shows fallback text, red flags section renders distinctly.</done>
</task>

</tasks>

<verification>
- `node --test src/__tests__/dashboard.test.js` — all query and template tests pass
- `node --test "src/__tests__/*.test.js"` — full suite still green (no regressions)
</verification>

<success_criteria>
- queries.js exports listOpportunities and getOpportunityDetail with correct SQL JOINs
- templates.js exports renderLayout, renderFeedPage, renderDetail, esc with correct HTML output
- All dashboard tests pass covering both DASH-01 and DASH-02 behaviors
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard/05-01-SUMMARY.md`
</output>
